# All Craft

This private repository consolidates 163 static Craft projects. Each source repository is preserved as an independent project under `projects/<repository-name>/` so that common filenames such as `index.html`, `script.js`, `styles.css`, and the `image/` directory do not collide.

The original repositories remain unchanged. See `SOURCE_REPOSITORIES.tsv` for the complete source mapping.
